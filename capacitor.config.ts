import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ryslea.com',
  appName: 'app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
